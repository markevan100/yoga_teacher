import React from 'react';
import classes from './About.module.css';

const about = props => (
    <div className={classes.About}> Hi, I'm a yoga teacher and blogger and I'm into gut biomes.</div>
);

export default about;
